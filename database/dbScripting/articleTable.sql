create table articleTable(

	source VARCHAR(255),

	article VARCHAR(255),

	title VARCAHR(255),

	description VARCHAR(255),

	url VARCHAR(255),

	urlToImage VARCHAR(255),

	publishedAt VARCHAR(255),

	content VARCHAR(255)
);
